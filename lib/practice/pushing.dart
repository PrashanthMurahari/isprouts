import 'package:flutter/material.dart';
class Pushing extends StatelessWidget {
  const Pushing({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('hi'),
      ),
    );
  }
}
